#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    // Declare some variables
    int a = 1, b = 2;
    float f = 1.23f;
    char c = 'b';

    // Use printf display text on the screen
    printf ("The variables are\na = %d\nb=%d\nf=%f\nc=%c\n", a, b, f, c);

    // Exit from main
    return 0;
}